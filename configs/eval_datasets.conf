# ===== Evaluation dataset path configuration =====
# Per-dataset variables: _JSONL, _IMAGE, _DEPTH, _SAMPLES (0 = all)
# Paths are relative to the directory where eval.sh is invoked (project root).

ANNO_ROOT="./annotations"
DEFAULT_DATA="./datasets"

# ===== Indoor =====

SCANNETPP_JSONL="${ANNO_ROOT}/scannetpp_pixel_depth_val.jsonl"
SCANNETPP_IMAGE="${DEFAULT_DATA}"
SCANNETPP_DEPTH="${DEFAULT_DATA}"
SCANNETPP_SAMPLES=0

SUNRGBD_JSONL="${ANNO_ROOT}/sunrgbd_pixel_depth_test.jsonl"
SUNRGBD_IMAGE="${DEFAULT_DATA}"
SUNRGBD_DEPTH="${DEFAULT_DATA}"
SUNRGBD_SAMPLES=0

IBIMS1_JSONL="${ANNO_ROOT}/ibims1_pixel_depth_test.jsonl"
IBIMS1_IMAGE="${DEFAULT_DATA}"
IBIMS1_DEPTH="${DEFAULT_DATA}"
IBIMS1_SAMPLES=0

NYUV2_JSONL="${ANNO_ROOT}/nyuv2_pixel_depth_test.jsonl"
NYUV2_IMAGE="${DEFAULT_DATA}"
NYUV2_DEPTH="${DEFAULT_DATA}"
NYUV2_SAMPLES=0

# ===== ETH3D =====

ETH3D_JSONL="${ANNO_ROOT}/eth3d_pixel_depth_all.jsonl"
ETH3D_IMAGE="${DEFAULT_DATA}"
ETH3D_DEPTH="${DEFAULT_DATA}"
ETH3D_SAMPLES=0

# ===== Outdoor =====

ARGOVERSE2_JSONL="${ANNO_ROOT}/argoverse_pixel_depth_test.jsonl"
ARGOVERSE2_IMAGE="${DEFAULT_DATA}"
ARGOVERSE2_DEPTH="${DEFAULT_DATA}"
ARGOVERSE2_SAMPLES=0

WAYMO_JSONL="${ANNO_ROOT}/waymo_pixel_depth_test.jsonl"
WAYMO_IMAGE="${DEFAULT_DATA}"
WAYMO_DEPTH="${DEFAULT_DATA}"
WAYMO_SAMPLES=0

DDAD_JSONL="${ANNO_ROOT}/ddad_pixel_depth_val.jsonl"
DDAD_IMAGE="${DEFAULT_DATA}"
DDAD_DEPTH="${DEFAULT_DATA}"
DDAD_SAMPLES=0

NUSCENES_JSONL="${ANNO_ROOT}/nuscenes_pixel_depth_test.jsonl"
NUSCENES_IMAGE="${DEFAULT_DATA}"
NUSCENES_DEPTH="${DEFAULT_DATA}"
NUSCENES_SAMPLES=0
